{% set name = "boltons" %}
{% set version = "26.1.0" %}
{% set hash = "dd67de2cb54ea66367bb4bb4a8b030ce5d34c87771971dac6dd5b3982ab3d6c3" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: boltons-{{ version }}.tar.gz
  url: https://github.com/mahmoud/boltons/archive/{{ version }}.tar.gz
  sha256: {{ hash }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - pip
    - python {{ python_min }}
    - flit-core >=3.2,<4
  run:
    - python >={{ python_min }}

test:
  source_files:
    - boltons
    - tests
    - pyproject.toml
  requires:
    - pytest
    - python {{ python_min }}
  commands:
    - pytest --doctest-modules boltons tests

about:
  home: https://github.com/mahmoud/boltons
  license: BSD-3-Clause
  license_file: LICENSE
  summary: >
    boltons should be builtins. Boltons is a set of over 160 BSD-licensed, pure-Python utilities in the same spirit as--and yet conspicuously missing from--the standard library.

extra:
  recipe-maintainers:
    - kalefranz
    - nicoddemus
